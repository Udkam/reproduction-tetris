# T31 — Mutation Gravity, Ice, and Status Clarity

Status (2026-08-05): **IN PROGRESS** from `main@7392edf`.

## Objective

Reduce Mutation to four coherent carrier effects, make Supergravity guidance and lock
geometry share one latched rule, and replace rejected visual/audio feedback without
changing unrelated game systems.

## Phase A — contract

- Freeze the four-item pool, timer-or-piece-latch semantics, local activation/clear
  grammar, frameless status ledger, reduced-motion endpoint, and exact changed paths.
- Commit only `docs/DESIGN.md`, `docs/CURRENT_TASK.md`, this file, and `progress.md`.

## Phase B — Core and presentation

- Remove Reshape from the type, deterministic pool, activation branch, live rule copy,
  and direct tests.
- Make projected Supergravity landing columns depend on the same `timer active OR
  piece latch` predicate used by lock resolution.
- Add regression coverage for active timer, expired-but-latched, expired-unlatched, and
  post-lock latch reset.

## Phase C — feedback and status

- Replace Ice activation/clear with localized frost binding and faceted release.
- Replace Supergravity activation/clear with localized downward compression; replace
  the spike trail with clipped soft cell echoes.
- Remove the Supergravity loop, expiry cue, and special landing feedback while keeping
  the ordinary lock contract and short activation cue.
- Restore localized item name, seconds, signal, and meter as frameless ledger rows;
  retain an empty idle state.

## Phase D — evidence and acceptance

- Run focused tests during each source checkpoint. After the final source edit, run one
  typecheck, complete suite, production build, and one bounded Chromium evidence pass.
- Capture Ice activation, Supergravity before and after timer expiry on the same piece,
  the resulting ghost/lock agreement, simultaneous status rows, reduced motion, one
  Canvas, zero DOM board cells, zero console errors, and teardown.
- Obtain independent read-only QA of the exact candidate range before changelog closure,
  scoped pre-push scanning, and publication.

## Rollback

Revert the status checkpoint to restore only the prior ledger styling. Revert the
feedback checkpoint to restore prior Mutation visuals/audio without touching Core.
Revert the Core/presentation checkpoint to restore the five-item pool and prior ghost
rule. Contract/evidence/closure commits remain independent.
