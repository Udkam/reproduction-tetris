# Phase 12 — Material Truth and Authored Learning

Status: `ACCEPTED / CLOSED / FINAL PUBLICATION CHECKPOINT`

Final-QA retry resolved: the procedural bedrock wall remains accepted and frozen.
Puzzle title glyph boxes and the `844x390` Settings footer now fit without clipping,
the exact mastery certificates pass, and the final browser matrix includes active
Mutation plus dynamic one/two-stone Survival evidence.

Base: `main@ac11e016d459a82bd2242650632cd8a1c936c253`

## Goal

Replace five rejected outcomes without reopening accepted mechanics: align leave
color/focus semantics, make Settings one continuous console, install a new locally
packaged English/data type system with complete layout fit, replace decorative rock
with flat/square solid geology at 4x cadence, and turn Puzzle guidance from live
enumeration into route-backed introductory technique lessons.

## Checkpoint sequence

| Checkpoint | Boundary | Acceptance before moving on |
| --- | --- | --- |
| P12.0 | Contract and evidence inventory | Five outcomes, local fonts, material grammar, lesson rules, resources, and browser matrix are explicit |
| P12.1 | Shared UI and typography | Leave color/focus agree; Settings is one surface; Barlow Semi Condensed/Fira Code are local; bilingual fit tests pass |
| P12.2 | Survival Core and Renderer | Integer 4x cadence; flat shelf; square one/two-stone events; unresolved stone contact cannot lock an ordinary piece; focused replay/geometry tests pass |
| P12.3 | Puzzle curriculum | Live analysis removed; selected intro lessons are concise and backed by public-command routes, including timed anchor side-slip |
| P12.4 | Final source gates | One typecheck, one single-worker full suite, one production build from the final source |
| P12.5 | Browser evidence | Required interactions, modes, languages, viewports, reduced motion, and error/overflow assertions |
| P12.6 | QA and publication | Immutable candidate, independent read-only verdict, scoped scans, acceptance docs, non-force push, resource release |

## Browser evidence matrix

- Leave dialog: Chinese and English; default focused left return action is filled;
  arrow selection reaches the pale right stay action; Enter/Escape paths.
- Settings: Rules, Controls, Keyboard, and Record in one uninterrupted surface at
  `1440x900`, `390x844`, and `844x390`; no child card boxes or structural void.
- Typography: Home, Settings, all four HUDs, results, and Puzzle library after
  `document.fonts.ready`; full `s/cell`, complete buttons, stable baselines, no runtime
  font request, clipping, overlap, disappearance, or unintended ellipsis.
- Survival: three entry beats, flat bedrock contact edge, one square stone, two stacked
  square stones, ordinary-piece comparison proving 4x cadence, an ordinary piece
  riding dynamic stone support without locking, settled clearable stone,
  desktop/portrait, and reduced motion.
- Puzzle: selected intro lesson before play, no live analysis card, unchanged two-piece
  Next, ordinary intro route, anchor side-slip route, alternate completion route, undo,
  and clean levels without a lesson card.
- Every gameplay frame: exactly one Canvas, zero DOM board cells, zero document/panel
  overflow, zero console errors, and zero page errors.

## Resource boundary

- One production writer. Independent QA is read-only only after an immutable candidate.
- No Serena, WMI/CIM, persistent indexer, watcher, language server, or inspection-only
  browser. Focused tests use one worker and no file parallelism.
- At most one owned Vite server and one managed browser batch may exist during a
  bounded visual pass. Record ownership and port, then terminate and verify release.
- Full suite and build run once after the last source edit. Exact-path staging only;
  each green subsystem is committed before the next shared subsystem opens.

## Current evidence inventory

- Existing Core artifacts replay two distinct completion routes for all fifty levels
  through public commands and preserve immutable anchors. They are evidence inputs,
  not player-facing solutions.
- Existing live `PuzzleGuidancePanel` performs bounded landing enumeration, but direct
  review rejects the resulting metrics dashboard. It will be retired from product UI.
- Fontsource registry confirms Barlow Semi Condensed and Fira Code packages at `5.3.0`
  under OFL-1.1. Installation, notices, and license files belong to P12.1.
- The initial inventory contained no Phase-12 acceptance artifact; the completed
  immutable source/evidence/QA chain is recorded below.

## Final acceptance

- Frozen product source: `d84b04351dc89d5f503df2112a71789950ba0796`.
- Final evidence: `18f8886..9e22a8b`; the static manifest contains sixteen screen
  groups and the dynamic manifest contains four groups with twenty hashed PNGs. Both
  bind to the frozen source and report zero console/page errors.
- Survival proof covers deterministic one/two-stone events, exact four-times cadence,
  temporary non-locking support, and atomic stone-above push. The accepted procedural
  wall remains `2e14ec3 / dcb1d79 / 5619bce / 92a124f` and was not modified.
- Mutation proof covers all four named activations, persistent Ice, Supergravity and
  Multiplier states, cleared-carrier cleanup, and the airborne Supergravity landing
  latch. Each activation screenshot's renderer item matches its filename; the
  multiplier field records `600` ticks at factor `2`.
- Strict exact Puzzle certificates pass `3/3`. Final typecheck passes; the full suite
  passes `32` files with `1` skipped (`297` tests with `3` skipped); production build
  passes with `762` modules and only the existing bundle-size advisory.
- Independent read-only QA `9ec1149..6fce9e6` reports P0–P3 and evidence gaps all
  zero. A scoped redacted gitleaks scan of the eight pre-closeout commits reports no
  finding; the lockfile OSV scan previously covered `158` packages with no issue.
- Owned browser/server resources are released and port `5178` is free. Publication is
  coordinator-owned, non-force, and requires exact local/tracking/remote equality.
