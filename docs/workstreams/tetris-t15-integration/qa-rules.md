# Phase 8 rules QA

- Candidate: `a599f4c`
- Product baseline: `4e4cca1`
- Verdict: **ACCEPT**
- Findings: `P0=0 / P1=0 / P2=0 / P3=1 / GAP=0`
- Method: read-only static audit; no npm command, browser, server, solver, or
  persistent Node process was started.

## Accepted claims

- `4e4cca1..a599f4c` contains no product, dependency, or configuration drift.
  The rejected selector experiment at `dce331b` is not in the candidate
  ancestry.
- Ordinary line clear uses the restored stationary nine-tick centre-out
  sweep. Survival, Mutation, and later Classic presentation cues remain
  available.
- Mutation preserves independent carrier generation, side-effect-free Next
  prediction, all 7 piece shapes with all 4 carrier effects, 10-second status
  refresh, one-cell-per-second Ice drop, the 0.1-second-per-cell speed floor,
  multi-status FIFO handling, 2x-to-4x multiplier stacking, one-shot Bomb,
  and bounded Collapse work.
- Puzzle contains 50 schema-7 levels. Every level has two distinct replayable
  public-Core solution routes, progressive unlocking, fail-closed persistence
  migration, and direct `Z` undo without a confirmation dialog.
- Phase 8 evidence binds the product baseline, harness, raw evidence, four
  modes, single-canvas lifecycle, responsive views, Puzzle dual Next,
  Mutation carrier preview, and Survival bedrock.

## Accepted low-severity note

- `P3`: with three simultaneous Mutation statuses at a narrow width, long
  status text can be ellipsized. Iconography, material treatment, timer, and
  progress remain distinguishable, so this does not block acceptance.
