# T12.5 Independent QA Verdict

Candidate source: `d2469e3f66072d5edc905981200d0df90b06780f`
Reviewed range: `69eec5f..d2469e3`
Disposition: **ACCEPT**

## Core / deterministic-rules review

- Verdict: **ACCEPT**; no P0–P2 finding.
- Reviewed the active twenty-level curriculum, fixed seeds/seven-bags, target-only
  success, removal of budget terminals and active anchor requirements, and the
  Core-dispatched route fixture.
- Confirmed `KeyB → undo` restores the pre-lock board, targets, active block,
  queue/randomizer, timing, score, lines, and count; hard-drop score restoration,
  consecutive undo, paused undo, top-out recovery, and non-Puzzle no-op behavior are
  covered. Marathon/Survival hashes omit Puzzle undo history.
- Commands actually run by independent QA:
  - `npm.cmd exec vitest run src/game/core/engine.test.ts src/game/core/board.test.ts src/game/core/puzzles.test.ts src/game/core/puzzleCampaign.test.ts src/game/core/puzzleFlow.test.ts src/game/core/puzzleSolverResults.test.ts src/game/core/puzzleUndo.test.ts src/game/input/InputController.test.ts src/game/runtime/GameRuntime.test.ts src/game/runtime/qaScenario.test.ts` — 19 files / 102 tests passed.
  - `npm.cmd exec vitest run src/puzzleProgress.test.ts` — 3 files / 13 tests passed.
  - `git diff --check 69eec5f..d2469e3` — clean.

## Visual / browser review

- Verdict: **ACCEPT**; no P0–P2 finding.
- Fresh browser inspection at 1280×720, 390×844, and 844×390 confirms 20 records,
  3 initial opens / 17 sealed, all written gate rules, no card thumbnails or top-right
  dots, exactly one selected preview, zero page/console errors, and no document
  overflow. Mobile atlas scrolling reaches Level 20 above the selected detail panel.
- Completion of 01+02 opens 04–06; a valid chain through 16+17 opens 19–20. Gameplay
  exposes two Next pieces, shows the `B` cue and touch Undo, and both paths restore the
  pre-drop board/count/history. Renderer rollback clears discarded visual pulses and
  reduced-motion emulation removes atlas transitions.
- Independent QA used read-only Git/`rg` checks, `git diff --check`, and native
  Playwright against `http://127.0.0.1:5183`; it edited, staged, built, and committed
  nothing.

No source repair is authorized or needed from these verdicts. Coordinator acceptance,
progress/changelog integration, and publication may proceed.
