/**
 * TetraMorph Design System v1.0 — future-facing motion timing.
 * Phase 1 declares these values but intentionally introduces no new animation.
 */
export const ANIMATION = {
  easing: {
    standard: 'ease-out',
  },
  duration: {
    hover: 120,
    press: 80,
    modal: 220,
    page: 300,
  },
} as const;
